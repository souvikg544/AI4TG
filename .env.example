# Gradio API Configuration
# The app is pre-configured to use souvikg544/quickdraw-classifier
# You can override this by setting your own Gradio Space URL

# Example for using a different Gradio Space:
# REACT_APP_GRADIO_SPACE=your-username/your-space-name

# Optional: Number of top predictions to return (default: 5)
# REACT_APP_TOP_K=5

# Optional: API timeout in milliseconds (default: 30000)
# REACT_APP_API_TIMEOUT=30000 